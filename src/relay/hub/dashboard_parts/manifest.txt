# Ordered fragments assembled into the dashboard HTML shell at serve time.
# These cover the document open, the <style> sheet, and the body shell that ends
# with <script type="module" src="/static/dashboard/main.js">. The dashboard
# JavaScript is NO LONGER here — it lives as native ES modules under
# ../dashboard_modules/ (served read-only at /static/dashboard/). Edit a CSS or
# markup section by editing its fragment; edit behavior by editing a module.
00-doc-open.part.html
01-styles.part.html
02-body-shell.part.html
99-doc-close.part.html
